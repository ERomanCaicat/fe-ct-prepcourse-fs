function invertirArray(array) {
  // Invierte el arreglo array recibido por argumento.
  // Tu código:

  /*var reverso=[];
      for(let i=0;i< array.length;i++){        
        reverso.unshift(array[i]);
      }*/

      var reverso= array.reverse();

      return reverso;
    

}

module.exports = invertirArray;
